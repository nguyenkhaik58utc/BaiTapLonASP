@using EntityFW.EF6;
@model List<Role>

<select class="form-control"
        style="border-radius: 2em; margin-top: 0.3em;">
    @foreach (var item in Model)
    {
        <option>@item.Role_Name</option>
    }
</select>
